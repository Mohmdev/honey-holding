import {
  default as EnvironmentSettingsOwnershipPage,
  generateMetadata
} from '@root/app/(frontend)/(dashboard)/dashboard/[team-slug]/[project-slug]/(tabs)/settings/ownership/page'

export default EnvironmentSettingsOwnershipPage

export { generateMetadata }
